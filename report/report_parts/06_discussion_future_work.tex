\section{Discussion \& Future Work}\label{sec:discussion}

\subsection{Chemist agent improvement}
In the current implementation, chemistry filtering and scoring are computed directly from peptide sequences.
An extension could incorporate richer molecular representations (for example SMILES-derived descriptors) as additional conditioning and scoring signals.
Such an extension would require a dedicated preprocessing pipeline, a clear descriptor-selection strategy, and retraining of the generation components to ensure consistency between conditioning variables and chemistry constraints.

\subsection{LLM usage for input}
Initially, we considered using a large language model to guide peptide selection by training it to understand and communicate preferences about desired peptide characteristics. 
This approach could have provided an intuitive interface for specifying optimization targets and constraints.
This LLM-based input method remains a promising avenue for future work, particularly for enhancing the usability and flexibility of the system when dealing with complex or evolving biological requirements.

\subsection{Peptide-Specific Biological Scoring}\label{subsec:future_biologist}
Domain feedback from the biologist highlighted a central limitation of the current study: biological accuracy cannot be validated without experimental assays. In other words, the present biology signal is useful for optimization guidance, but it is not a biological ground truth.

The main improvement is therefore to replace the current proxy with a model trained (or fine-tuned) specifically on peptides and supported by peptide-level validation data. Ideally, this model should score or classify a peptide conditionally on a target (or target-related context), instead of relying on a general protein-space similarity signal.

The expected benefit is a more accurate and reliable biological signal for ranking candidates in the loop. A peptide-specialized model could also provide richer outputs than a single proxy score, for example clearer estimates of expected activity and additional information about risk or toxicity. Building such a model is likely a substantial research project on its own, but it is the most direct path to strengthen biological validity in future versions of the pipeline.

In short, our current biologist agent is a practical baseline for iterative optimization, while a target-conditioned peptide model is the natural next step to improve biological relevance and decision quality.
